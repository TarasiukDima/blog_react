export enum Theme {
  LIGHT = "light_theme",
  DARK = "dark_theme",
}

export enum ButtonSize {
  S = "size_s",
  M = "size_m",
  L = "size_l",
}

export enum OrderVariants {
  ASC = "asc",
  DESC = "desc",
}
